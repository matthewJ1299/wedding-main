# Baked into the bundle by Create React App when you run `npm run build`.
# Do not commit secrets here; only public URLs and non-sensitive build-time values.
REACT_APP_API_URL=https://api.matthewandsydney.co.za
REACT_APP_SITE_URL=https://matthewandsydney.co.za
GENERATE_SOURCEMAP=false
