# Optional override: publish ports on the host for laptop testing.
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up --build

services:
  backend:
    ports:
      - "3001:3001"

  frontend:
    ports:
      - "8080:80"
